pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
            }
        }
        stage('Run sh') {
            sh 'run.sh'
        }
    }
}
